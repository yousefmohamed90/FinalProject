{
  "name": "codesourcehub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --legacy-peer-deps --prefix frontend",
    "dev": "concurrently --kill-others-on-fail --names \"BACK,FRONT\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "@types/multer": "^2.1.0",
    "concurrently": "^9.1.0"
  },
  "dependencies": {
    "cloudinary": "^2.10.0",
    "multer": "^2.1.1"
  }
}
